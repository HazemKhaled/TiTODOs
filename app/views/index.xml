<Alloy>
  <Collection src="tasks"/>
  <Window>
    <ListView defaultItemTemplate="template" id="list" onItemclick="productListClicked">
      <Templates>
        <ItemTemplate name="template">
          <View class="container">
            <ImageView bindId="image" class="imageViw"/>
            <View class="lineView">
              <Label bindId="myDataId" class="myDataIdLbl"/>
              <Label bindId="text" class="textLbl"/>
              <Label bindId="lastModifiedDate" class="lastModifiedDateLbl"/>
            </View>
          </View>
        </ItemTemplate>
      </Templates>
      <ListSection dataCollection="tasks">
        <ListItem image:image="{image}" lastModifiedDate:text="{lastModifiedDate}" myDataId:text="{id}" text:text="{text}"/>
      </ListSection>
    </ListView>
  </Window>
</Alloy>
